import React from 'react';
import './AddButton.scss';

const AddButton = () => {
    return (
        <div className="AddButton">
               <a href="/">+</a>
        </div>
    );
};

export default AddButton;