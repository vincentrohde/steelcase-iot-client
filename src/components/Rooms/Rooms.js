import React from 'react';
import Room from '../../components/Room/Room';
import './Rooms.scss';

const Rooms = () => {
    return (
        <div className="Rooms">
            <Room />
        </div>
    );
}

export default Rooms;